<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      No permissions are required: lock/unlock detection uses the
      ACTION_SCREEN_OFF / ACTION_USER_PRESENT system broadcasts, and
      foreground/background detection uses the activity lifecycle.
    -->
</manifest>
